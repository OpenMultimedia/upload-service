Sistema de subida de archivos vía API
---------

INSTALACIÓN
-----------

Se requiere Apache2 con mod_ssl, mod_rewrite y soporte para PHP 5.3

Se deben copiar los archivos:
- htaccess.dist (renombrado como .htaccess)
- delete.php
- download.php
- upload.php

Y las carpetas:
- lib
- files (sólo si se usará la ruta de subida por defecto)

a la ruta de archivos web (p. ej. /var/www/ )

Se puede renombrar el archivo [lib/config.user.php.dist] a [lib/config.user.php] y editarlo de acuerdo a las necesidades.
